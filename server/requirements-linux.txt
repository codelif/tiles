# Linux inference now runs through the standalone llama-server binary
# (see scripts/fetch_llama_server.sh), not an in-process Python package.
# Base server deps come from pyproject.toml via `uv sync`.
